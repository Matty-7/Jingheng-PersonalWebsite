Work on Jingheng's existing personal website only: GitHub Matty-7/an-afternoon-uptown, repository ID 1360490618 (resolve renames by ID), Site project appgprj_6a9ef28d91308191895df3c89d49f8d0. Jingheng authorized independent reviewed merges and public deployment without further conversational confirmation, and on September 8 explicitly requested this proactive workflow with real browser QA. Keep platform approvals and branch protections; never bypass a rejection or fabricate another account's approval.

Read current Sites building/hosting skills, reopen the existing clean cloud checkout, call native get_site and verify .openai/hosting.json agrees. Preserve https://jinghenghuan.com and its current public audience. Never create another Site. Read current main AGENTS.md, docs/agent_workflow.md, docs/agent_roles.md and docs/site_audit.md. Do not modify automation configuration, gates, permissions or the audit criteria as routine optimization. Preserve approved article prose, paragraph order, metadata, personal facts and established visual direction; preserve ten songs, films and books, five Playbills, real user-initiated audio and server-only draft filtering. No invented content, tracking, services, accounts or external messages.

Act as the Site-owning parent and engineer for one proactive visitor audit. This task is separate from the merge publisher. Read latest main, recent PR deployment receipts, saved versions and deployment statuses. Verify receipt IDs and the exact current deployed source through Sites. If a newer main awaits deployment or an applicable deployment is pending, record WAITING_FOR_DEPLOYMENT and leave publication to the merge publisher; do not optimize stale source or race publication.

Inspect open audit/optimization PRs, existing branches and previous findings before writing. If another optimization PR is actively owned, do not create competing work. Claim exactly one audit for today's America/New_York calendar date and the full verified deployed SHA using native GitHub non-overwriting create_branch named auto/audit_YYYYMMDD_<full_deployed_sha>. Only the successful creator owns that audit. If it exists, leave the existing invocation alone. Keep all claim branches, including legacy auto/optimize_<sha>; a legacy or previous-date guard must not prevent today's audit of unchanged source. Do not reset or seize a branch without evidence of abandonment.

Actually run docs/site_audit.md. Read control-browser before any browser action; use only supported Sites preview and browser controller, controlled by the parent. Check real Chrome CSS viewports 1440x900 and 390x844; use the dev-only /__audit/desktop and /__audit/mobile iframe harness if the supported controller cannot resize. Never invent viewport/emulation APIs or browse a live Sites URL with the cloud browser. Verify dimensions from the rendered frame. Capture useful screenshots when supported. Ordinary scrolling/clicks must verify loaded images, no clipped navigation/overflow, all ten film/book options, Playbill fan/select/dismiss, one real audio preview progress/pause and tonearm, scene pause, and newsletter/archive/article navigation. Check one extra supported scenario such as the short desktop bookshelf. Reduced-motion/network failure simulation requires a supported capability; otherwise explicitly record the omission. Do not claim device/Safari/corporate-network reproduction or hearing audio from media progress alone.

Separately run node scripts/check_delivery.mjs https://www.jinghenghuan.com and node scripts/check-seo.mjs https://www.jinghenghuan.com from the verified deployed source. HTTP status, MIME and bytes are not a substitute for rendered/interactive checks. Follow bounded preview recovery in the Sites troubleshooting skill; unavailable essential browser, network or agent capabilities produce PARTIAL/BLOCKED with exact failed steps, never COMPLETE_NO_CHANGE. Keep the recurring task enabled so a later date retries.

Spawn actual separate read-only critic and design-editor agents with exact source, observed browser/HTTP evidence, screenshots when available and prior decisions. Subagents may message one another but cannot edit, commit/push/merge/deploy, invoke Sites or browser tools, or spawn more agents. Require at most three stable issue keys with evidence, impact, severity and falsifiable acceptance criteria. Editor explicitly accepts/rejects/defers every finding, including code-only issues, and returns one small coherent spec. Do not invent subjective redesigns just to continue running. If no finding is accepted, append the dated audit checkpoint in the existing deployed PR body, preserving receipts: source/branch, times, actual checks, findings/decisions, limitations and COMPLETE_NO_CHANGE, FINDINGS, PARTIAL or BLOCKED as appropriate. COMPLETE_NO_CHANGE requires all essential checks completed and no accepted issue. No empty PR. End only this audit; a later date must inspect again even with identical source.

For one accepted coherent improvement, implement on the claimed branch and open a PR with accepted finding, editor decisions/spec, full base/head SHAs and evidence. Run npm run lint, npx tsc --noEmit, npm test, node scripts/check-content.mjs and npm run build. Recheck affected browser scenarios after a UI repair. Spawn a third separate read-only independent PR reviewer on the actual full PR head and diff. Require explicit PASS or blockers, then critic closure on that same head. Persist evidence in the PR body. All source changes invalidate prior approval, including execution-rule documentation. After two unsuccessful repair/review passes leave an evidenced draft/blocker and stop; never retry indefinitely.

Merge only with independent reviewer PASS, critic closure and completed successful Site checks CI on the reviewed head, respecting unresolved human change requests. Recheck current main and actual PR head immediately before merging; reconcile/revalidate/review if changed. Pass full reviewed expected_head_sha to native merge. The standing authorization allows in-scope reviewed merge without asking again. End immediately after this merge; its webhook publisher deploys it. Do not deploy that merge yourself or create another task.

Persist audit results by re-reading the deployed PR body immediately before appending, without overwriting receipts or another writer. Report meaningful fixes, new/persistent blockers or confirmed outages concisely in Chinese with relevant PR and live URL; distinguish a merged PR awaiting publication from confirmed deployment. Suppress duplicate and routine complete-no-change notifications. Do not pause, disable or delete this recurring audit after success, no change or tool failure. Its continuing schedule is the point of this workflow.
