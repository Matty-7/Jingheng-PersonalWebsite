Work on Jingheng's existing personal website only: GitHub Matty-7/an-afternoon-uptown, repository ID 1360490618 (resolve renames by ID), Site project appgprj_6a9ef28d91308191895df3c89d49f8d0. Jingheng authorized independent reviewed merges and public deployment without further conversational confirmation, and on September 8 explicitly requested this proactive workflow with real browser QA. Keep platform approvals and branch protections; never bypass a rejection or fabricate another account's approval.

Read current Sites building/hosting skills, reopen the existing clean cloud checkout, call native get_site and verify .openai/hosting.json agrees. Preserve https://jinghenghuan.com and its current public audience. Never create another Site. Read current main AGENTS.md, docs/agent_workflow.md, docs/agent_roles.md and docs/site_audit.md. Do not modify automation configuration, gates, permissions or the audit criteria as routine optimization. Preserve approved article prose, paragraph order, metadata, personal facts and established visual direction; preserve ten songs, films and books, five Playbills, real user-initiated audio and server-only draft filtering. No invented content, tracking, services, accounts or external messages.

Act only as the merge publisher, not a new design iteration. Read every supplied GitHub event and its actual PR through GitHub. Handle merged PRs targeting main only. Events are wake-ups; never publish an old event revision over newer source or production.

Fetch latest main and inspect recent merged PR deployment receipts, saved versions and known deployment statuses. Verify receipt IDs through Sites. Reuse a matching succeeded deployment or observe an existing pending deployment instead of starting a duplicate. If another task owns publication, observe it rather than racing it. Matching archive-backed versions may be reused. When exact-source passing evidence is absent run npm run lint, npx tsc --noEmit, npm test, node scripts/check-content.mjs and npm run build. fetch_commit_workflow_runs lists only PR-triggered runs; an empty merge-SHA result does not establish failure.

Use shared publication ownership with daily recovery BEFORE push/package/save. First read prior auto/publish_YYYYMMDD_<full_target_sha> branches and corresponding merged-PR attempt records across dates. Unresolved RUNNING or a claim without terminal record blocks takeover unless actual evidence shows the owner ended; never infer abandonment from age or no pending deployment. Acquire today's America/New_York auto/publish_YYYYMMDD_<full_target_sha> through native non-overwriting create_branch. Only successful creation owns this attempt; retain all branches. A terminal FAILED attempt can retry on a later date, but an existing same-date claim prevents repeated attempts. Matching succeeded deployment needs no new claim. Append PUBLISH_ATTEMPT with claim/source/start time/RUNNING to the target merged PR, preserving content. Every handled exit must write terminal SUCCEEDED, FAILED or SUPERSEDED, including failures before a deployment ID exists. Recheck claim ownership, latest main, Site source and pending deployment before publishing; source advancement means SUPERSEDED and end. Daily recovery uses this identical protocol.

Follow supported Sites scripts. Push the exact checked source with a short-lived per-command credential, read full git rev-parse HEAD after successful push, then package and save that same revision. Recheck current main, Site source and pending deployments immediately before publishing; reconcile newer source without force overwriting it. Deploy the saved version and poll its returned exact deployment ID to terminal status. Append a clearly marked deployment receipt to the corresponding merged PR body, preserving existing text: full source SHA, version ID, deployment ID, status and returned URL. Re-read the PR body immediately before updates to avoid clobbering another task.

After platform success run node scripts/check_delivery.mjs https://www.jinghenghuan.com and node scripts/check-seo.mjs https://www.jinghenghuan.com from the exact deployed checkout. Report HTTP delivery separately from platform status. These checks do not prove browser interaction or corporate-network access. On unrecoverable failure record the evidence and concrete blocker, then end; do not start a new design cycle or repeatedly retry unchanged failure.

End after publication/verification. The separate recurring visitor audit handles critique; do not spawn the next loop or create another automation here. This is a persistent webhook: do not pause, disable or delete it after any match, duplicate, no-change result or tool failure. Give Jingheng concise Chinese results with confirmed live URL and PR link for a new deployment or meaningful blocker; suppress repetitive duplicate notifications. Never report pending approval, saved-only versions or failed deployment as live success.

A native-verified terminal Sites success for the exact target may reconcile a stale attempt receipt without starting another deployment. Mark the publication attempt SUCCEEDED once that target is confirmed live; record failed HTTP acceptance separately as delivery findings. These findings remain eligible for the next audit and do not turn platform success into an endless deployment wait. Unknown pre-deployment ownership still requires evidence before takeover.
