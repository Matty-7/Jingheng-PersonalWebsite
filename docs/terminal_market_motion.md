# Desk terminal screens

## 2026-09-10: complete screen surfaces

Jingheng requested the black/amber, dense mortgage analytics language of his supplied terminal references, and rejected three visibly separate animated rectangles. Critic/editor accepted TERMINAL_SURFACE_SEAMS, TERMINAL_ANALYTICS_IDENTITY and TERMINAL_STATIC_CONTINUITY.

The illustration, hardware, mouse, room and image URL are unchanged. Two SVG screen surfaces fill and clip to the inner bezel quadrilaterals on the original 1672 × 941 artboard. Local affine transforms align the compact UI to the angled monitors. Left: rate-shift yield table and OAS/static analytics. Right: cashflow schedule and yield curves. These are decorative synthetic figures, not a live feed, pricing tool or reproduction of the uploaded office photos or instrument identifiers.

Five numeric cells use staggered discrete readings on a 12-second cycle. Two curves change vertical scale by at most 4% over 16/19 seconds, with fixed plot baselines. No full-panel flashing, large histogram motion, timers, new requests or dependencies. The existing pause, visibility and document-hidden hook controls animation. Loaded screen surfaces persist before observer initialization and when paused; reduced motion disables their animations and displays the first readings. Missing background hides the surfaces. Browsers without container-unit support retain the existing static bitmap fallback.

Parent browser checks used supported Chrome preview with measured 1440 × 900 and 390 × 844 iframe viewports (scrollbars consume 15px). Both showed full monitor surfaces without isolated patch edges, old chart bleed or bezel overlap; surrounding room/hardware preserved. Actual pause/resume changed computed animation states and retained the screen composition. Sampled numeric cells each showed exactly one visible reading; curve transforms changed subtly when running and held when paused. This is source-preview evidence, not real iPhone/Safari or corporate-network testing. Reduced-motion and missing-observer/network simulations were unavailable and remain source-reviewed limitations.
