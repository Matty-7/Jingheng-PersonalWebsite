# Personal publishing and object motion — September 8, 2026

Current follow-up: `collection_refinements.md` records semantic name colors, the restored podcast artwork, more compact collections, inward stylus position and removal of both desk ornaments. That newer owner request supersedes those details below.

This owner-requested update supersedes the project-shelf decision in `personal_room_edit.md`. Jingheng removed the entire project section and both podcast episode tiles. GitHub and LinkedIn remain footer destinations. Approved essays, paragraph order, publication metadata and the three ten-item collections remain untouched.

## Accepted decisions

- `personal_publishing_hierarchy`: YouTube, Podcast and Newsletters share one equal three-column section, stacking below 850px. Matty Huan, Talking Laughs and Newsletters have the same card anatomy and bottom-aligned actions. Home navigation is Channels, Newsletters, Music, Films, Books, Broadway. Visible section numbers run 01–05. `/journal` and all existing article URLs remain unchanged; non-article presentation and RSS channel naming use Newsletters.
- `interactive_objects_state`: The Playbill state starts null in server-rendered markup. Selecting either a cover or a named control pulls one card forward. Selecting it again, pointer-down on surrounding space, or Escape collapses the hand. These listeners never cancel another control's event or move focus. The fixed stage prevents the following desk from jumping.
- `interactive_objects_state`: The hero types and deletes Jingheng / Huan and 郇 / 敬恒, using the owner's formal Chinese name order. Completed names hold for four seconds; typing/deletion takes 120/70ms per character. The existing hero pause control also governs the name. Timers stop outside the viewport, in hidden documents, on manual pause and under reduced motion. Inactive states show a complete name, with full English SSR content. A permanent screen-reader identity includes both names; animated letters are hidden from assistive technology and never announced live. Two reserved lines avoid height changes.
- `interactive_objects_state`: The main music player's original arm texture is an SVG-clipped layer over an imagegen-edited clean deck. Actual audio playback rotates it 27 degrees about source pivot (1217,275) over 650ms; pause, end and error return it to its rest. This is a simplified automatic lift/park interaction, not a claim about every physical record player. Reduced motion changes poses immediately. The single audio element and preview logic are unchanged.
- `desk_collection_fidelity`: Imagegen moved both brick ornaments to the keyboard's left and varied the screens with candlesticks, volume, a line, a pie, bars and a heatmap. Thin bezels and adjacent central silver mounts remain. A precise native SVG DUKE label corrects the generated jersey lettering; no real player identity is implied. Display data is decorative, never live market data.

## Links and assets

Apple Podcasts and Substack marks use Simple Icons' CC0 paths, retrieved from its official repository. The Talking Laughs RSS at https://feed.xyzfm.space/urynb8y6tke7 identifies Matty Huan and the Mandarin show. Bounded Apple catalog checks in US/CN/HK found no matching feed/creator; no owned Substack URL was verified. This does not prove those accounts do not exist. The podcast link therefore retains the known Xiaoyuzhou show, with an explicit accessible destination label. With `profile.newsletterUrl` null, the Substack-branded newsletter card/footer link to the local archive and make no subscription claim. The archive describes Substack as the intended future distribution channel. An actual owner-supplied show/publication URL can replace these destinations later; no guessed handle, generic search link or false signup form was added.

The closing desk is 1672 × 941, encoded as WebP. Its two masked refresh rectangles remain inside their existing source panels. The arm-free deck is 1448 × 1086. The image service returned painted checkerboard rather than alpha; its exterior is masked with the original deck's alpha silhouette. Moving hardware reuses exact original image pixels through an SVG clip and retains the fixed cueing lever. A second generated arm was rejected for dimensional drift and false transparency; it is not shipped. Format conversion did not repaint or composite raster artwork.

## Verification limits

Independent review identified `hero_pause_fallback`: browsers without container-query units hid the shared pause control but could still type the name. The name now uses the same container-unit support gate as that control, remaining fully static in this fallback. This repair preserves pause availability whenever typing runs.

Asset inspection and source-coordinate checks are not browser QA. No browser, screenshots of the rendered site, DOM interaction tests, audio listening or motion-performance measurement were performed. Required application lint, TypeScript, publication tests, content checks, production build and exact-head independent review are recorded in the PR.

Source-crop arithmetic passed at widths 320, 375, 760, 849, 1280, 1440, 1920, 2560 and 3840 for combined toy/terminal/keyboard bounds (365,278)–(908,605). At 27 degrees the stylus reaches (907.3,779.4), 392.5 source pixels from the CSS vinyl center and within its 398px radius. Homepage anchors resolve, removed UI strings are absent, and approved post bytes match the base revision.
